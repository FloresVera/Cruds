<div class="container">
<%= form_with(model: asignatura, local: true) do |form| %>
  <% if asignatura.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(asignatura.errors.count, "error") %> prohibited this asignatura from being saved:</h2>

      <ul>
        <% asignatura.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
  <label>Asignatura</label> <br>
  <%= form.text_field :asignatura, :class => 'form-control' %>
  </p>

  <p>
  <label>Codigo</label> <br>
  <%= form.text_field :codigo, :class => 'form-control' %>
  </p>

    <p>
  <label>Horas semanales</label> <br>
  <%= form.number_field :horas_semanales, :class => 'form-control' %>
  </p>

  <p>
  <label>Sentido</label> <br>
  <%= form.text_area :sentido, :class => 'form-control' %>
  </p>

  <p><label>Area de Formacion</label>
    <%= form.select(:area_formacion_id, options_from_collection_for_select(AreasFormacion.all, :id, :area_fomacion), {}, {:class => 'form-control'}) %> 
  </p>

  <%= form.submit "Enviar", disable_with: 'Submiting...', :class => 'btn btn-primary' %>

<% end %>
</div>