<div class="container">

<%= form_with(model: areas_formacion, local: true) do |form| %>
  <% if areas_formacion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(areas_formacion.errors.count, "error") %> prohibited this areas_formacion from being saved:</h2>

      <ul>
        <% areas_formacion.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

 <p>
 <label>Nombre</label> <br>
 <%= form.text_area :area_fomacion, :class => 'form-control' %>
 </p>

 <%= form.submit "Enviar", disable_with: 'Submiting...', :class => 'btn btn-primary' %>

<% end %>

</div>