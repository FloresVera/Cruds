
<div class="container">
<%= form_with(model: perfil_egresado, local: true) do |form| %>
  <% if perfil_egresado.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(perfil_egresado.errors.count, "error") %> prohibited this perfil_egresado from being saved:</h2>

      <ul>
        <% perfil_egresado.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

 <p>
 <label>Nombre</label> <br>
 <%= form.text_area :perfil, :class => 'form-control' %>
 </p>

<p><label>Asignatura</label>
<%= form.select(:asignatura_id, options_from_collection_for_select(Asignatura.all, :id, :asignatura), {}, {:class => 'form-control'}) %> 
 </p>

 <%= form.submit "Enviar", disable_with: 'Submiting...', :class => 'btn btn-primary' %>

<% end %>
</div>